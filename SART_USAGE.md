# SART 实验模式使用说明

## 概述

系统支持两种 SART（持续注意力反应测试）实验模式：

- **短时模式（short）**：5分钟低负荷采集（默认）
- **长时模式（long）**：25分钟疲劳诱发

## 命令行启动方式

### Windows 命令行（CMD）

```cmd
# 默认启动（短时5分钟模式）
python -m ui.main

# 显式指定短时模式
python -m ui.main --sart-mode short

# 启动长时25分钟模式
python -m ui.main --sart-mode long
```

### PowerShell

```powershell
# 默认启动（短时5分钟模式）
python -m ui.main

# 显式指定短时模式
python -m ui.main --sart-mode short

# 启动长时25分钟模式
python -m ui.main --sart-mode long
```

## 实验流程

### 完整流程

```
登录 → 设备校准 → 基线校准(30s) → SART实验(5min/25min) → 文本问答 → 血压测量 → 舒尔特方格 → 结束
  ⬇️        ⬇️           ⬇️              ⬇️                ⬇️         ⬇️         ⬇️       ⬇️
[无标记]  [无标记]    [索引0-1]       [索引2-3]         [索引4-5]   [索引6-7]   [索引8-9] [索引10]
```

**说明**:
- 时间戳索引与测试流程完全对应，按顺序递增
- 登录和设备校准不记录时间戳（属于准备阶段）
- 每个测试阶段记录开始和结束两个时间戳

### 时间戳标记点

根据实际代码实现，系统在以下**11个关键时刻**按顺序记录时间戳（call_index 0-10）：

| call_index | 阶段 | 说明 | 代码位置 |
|------------|------|------|----------|
| 0 | **基线校准开始** | 30秒眼开静息基线采集开始 | baseline.py:125 |
| 1 | **基线校准结束** | 基线采集完成（30秒后） | baseline.py:146 |
| 2 | **SART实验开始** | 持续注意力测试开始（短时5分钟/长时25分钟） | sart.py:179 |
| 3 | **SART实验结束** | SART测试完成 | sart.py:276 |
| 4 | **文本问答开始** | **第一个问题真正开始朗读时记录** | test.py:2590 ⚠️ |
| 5 | **文本问答结束** | 语音答题环节完成 | test.py:2619 |
| 6 | **血压测量开始** | 血压测试开始 | test.py:2653 |
| 7 | **血压测量结束** | 血压测试完成 | test.py:2664 |
| 8 | **舒尔特方格开始** | 注意力/高脑负荷测试开始 | test.py:2711 |
| 9 | **舒尔特方格结束** | 舒尔特测试完成 | test.py:2722 |
| 10 | **系统结束** | 所有测试完成，保存时间戳文件 | test.py:2779 |

**⚠️ 重要修正（2025-10-24）**：
- **call_index 4（文本问答开始）** 的记录时机已从页面切换时（`start_test()` 函数开头）修正为**第一个问题真正开始朗读时**（`_speak_current_question()` 第一次调用）
- **修正原因**：之前在页面刚切换时就记录了时间戳，但此时页面还未完全显示，摄像头、多模态采集等设备都未完全启动，不符合"文本问答真正开始"的语义
- **现在的行为**：只有在第一个问题真正开始朗读时才记录，确保所有设备都已就绪，用户真正进入文本问答阶段
- **Q键跳过时**：如果在第一个问题朗读前就按Q键跳过，会在跳过时补记录开始时间戳（与结束时间戳相同或极接近）

**注意**: 
- 时间戳按**实际测试流程顺序**严格递增记录
- call_index 与测试流程完全对应，0是第一个阶段（基线开始），10是最后（系统结束）
- 使用Q键跳过时，开始/结束时间戳会在同一时刻记录，但索引顺序不变

### 两种模式对比

| 特性 | 短时模式 (short) | 长时模式 (long) |
|------|------------------|-----------------|
| **时长** | 5分钟 (300秒) | 25分钟 (1500秒) |
| **用途** | 低负荷采集 | 疲劳诱发 |
| **时间戳索引** | 2 (开始), 3 (结束) | 2 (开始), 3 (结束) |
| **KSS评估** | 无 | 每5分钟一次 |
| **试次数量** | 约260个 | 约1300个 |

**重要**: 两种模式使用相同的时间戳索引（2和3）

## SART 任务规则

- **呈现**：随机显示数字 1-9
- **Go试次（88%）**：看到除"3"外的数字 → 按【空格】
- **NoGo试次（12%）**：看到"3" → **不要按键**
- **时间参数**：
  - 数字呈现：250ms
  - 空屏间隔：900ms
  - 总节奏：每个试次 1150ms

## 数据保存

所有时间戳保存在 `recordings/<用户名>/<时间戳>/eeg/part_timestamps.json`，格式：

```json
[
  {
    "timestamp": 1761296951.213,
    "datetime": "2025-10-24T17:09:11.213341",
    "call_index": 0
  },
  {
    "timestamp": 1761296953.996,
    "datetime": "2025-10-24T17:09:13.995737",
    "call_index": 1
  }
]
```

**字段说明**:
- `timestamp`: Unix时间戳（秒，浮点数）
- `datetime`: ISO 8601格式的本地时间
- `call_index`: 时间戳序号（0-10，对应上表）

**示例解读**（基于附件 `part_timestamps.json`）:
```
call_index 0 (17:09:11) → 基线校准开始
call_index 1 (17:09:14) → 基线校准结束 (3秒，使用Q键快速跳过)
call_index 2 (17:09:15) → SART开始 (1秒切换时间)
call_index 3 (17:09:42) → SART结束 (27秒，应为5分钟但Q键跳过)
call_index 4 (17:09:46) → 文本问答开始 (4秒切换时间)
call_index 5 (17:10:12) → 文本问答结束 (26秒答题)
call_index 6 (17:10:13) → 血压测量开始
call_index 7 (17:10:13) → 血压测量结束 (同一时刻，Q键跳过)
call_index 8 (17:10:14) → 舒尔特开始
call_index 9 (17:10:14) → 舒尔特结束 (同一时刻，Q键跳过)
call_index 10 (17:10:34) → 系统结束 (20秒保存数据)
```

**时间差分析**:
- 基线: 3秒 (Q键跳过，正常30秒)
- 基线→SART: 1秒切换
- SART: 27秒 (Q键跳过，正常5分钟)  
- SART→文本问答: 4秒切换
- 文本问答: 26秒
- 血压/舒尔特: Q键瞬间跳过
- 数据保存: 20秒

SART 实验结果保存在 `recordings/<用户名>/<时间戳>/sart/sart_events_<时间戳>.csv`：

```csv
trial,timestamp,digit,is_nogo,key_pressed,response_time,correct
1,1761296982.123,5,0,1,234.5,1
2,1761296983.273,3,1,0,,1
...
```

## 注意事项

1. **默认模式**：不加参数启动时使用短时5分钟模式
2. **切换方式**：只能通过命令行参数指定，不支持运行时切换
3. **数据完整性**：所有阶段开始和结束都会自动记录时间戳，无需手动干预
4. **时间戳顺序**：call_index 严格按测试流程递增（0=基线开始，10=系统结束）
5. **Q键跳过**：调试时按Q跳过阶段仍会记录时间戳，开始/结束时刻可能相同或很接近
6. **EEG同步**：时间戳用于与脑电设备数据对齐分析

## 故障排查

### 问题：启动后未识别 --sart-mode 参数

**解决**：确保使用正确的命令格式：
```cmd
python -m ui.main --sart-mode long
```

### 问题：时间戳未正确记录

**检查方法**：
1. 查看日志中是否有 `📍 已记录XXX时间戳` 的信息
2. 检查 `recordings/<用户名>/<时间戳>/eeg/part_timestamps.json` 文件
3. 确认应有11个时间戳（call_index 0-10）

### 问题：时间戳索引不连续或数量不对

**可能原因**：
- 使用Q键跳过部分阶段（正常现象，跳过阶段的开始/结束时刻相同）
- 系统异常退出未保存最后时间戳
- 查看日志文件定位具体问题

### 问题：想查看当前 SART 模式

**方法**：启动后在说明页面会显示模式信息（如"SART 任务 (低负荷采集, 时长: 5分钟)"）

### 问题：如何验证时间戳正确性

**验证脚本**：
```python
import json
from datetime import datetime

with open('recordings/.../part_timestamps.json') as f:
    data = json.load(f)

print(f"总时间戳数: {len(data)}")
for item in data:
    idx = item['call_index']
    dt = item['datetime']
    print(f"[{idx}] {dt}")

# 应输出 0-10 共11个时间戳
```
